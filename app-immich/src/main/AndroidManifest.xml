<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamilyAppifier">

        <!-- Launch the library's WebViewActivity directly! -->
        <activity
            android:name="de.kalass.familyappifier.library.WebViewActivity"
            android:exported="true"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Configure Wrapper parameters via Meta-Data -->
            <meta-data
                android:name="de.kalass.familyappifier.START_URL"
                android:value="https://immich.home.kalass.de" />
            
            <meta-data
                android:name="de.kalass.familyappifier.DOMAIN_WHITELIST"
                android:value="immich.home.kalass.de,*.immich.home.kalass.de" />
        </activity>

    </application>

</manifest>
